name: 17.2 - Custom actions (JS)

on:
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Echo title
        uses: ./.github/actions/title-logger
        with:
          title: "Testing my custom action!"
